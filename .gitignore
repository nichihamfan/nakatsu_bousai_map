# 開発時の中間生成物・作業データ（公開サイトの動作には不要）
# 避難所座標のジオコーディング過程で生成したバックアップ・ログ・巨大な未使用データ等。
# 先頭の/でリポジトリ直下のdata/のみに限定する（/public/data/はアプリ本体が使う
# 必須データなので、うっかりここも除外してしまわないよう注意）。
/data/

# Overpass API等の生レスポンスキャッシュ（数十MB、公開サイトには不要）
scripts/cache/

# OS・エディタ関連
.DS_Store
Thumbs.db
